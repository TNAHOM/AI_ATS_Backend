PROJECT_NAME=AI_ATS_Backend
DB_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/ai_ats
# Generate with a cryptographically secure source (example: 64-char hex from `openssl rand -hex 32`)
SECRET_KEY=REPLACE_WITH_SECURE_RANDOM_64_CHAR_HEX
GEMINI_API_KEY=your-gemini-api-key
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name
MAX_JOB_APPLICANT_RETRIES=3
# Get <your-clerk-subdomain> from Clerk Dashboard -> Configure -> Domains.
# Example issuer: https://<your-clerk-subdomain>.clerk.accounts.dev
# If you use a custom Clerk domain in production, set that exact domain here instead.
CLERK_ISSUER=https://<your-clerk-subdomain>.clerk.accounts.dev
# JWKS URL must use the same Clerk domain as CLERK_ISSUER (/.well-known/jwks.json)
CLERK_JWKS_URL=https://<your-clerk-subdomain>.clerk.accounts.dev/.well-known/jwks.json
# Required for production APIs: enforce audience validation to prevent token reuse across services.
# Must exactly match the audience configured in Clerk Dashboard -> JWT Templates -> <your-template> -> Audience.
# This is an identifier string and does not need to be a reachable URL (e.g., https://api.yourapp.com or api://your-app-identifier).
# IMPORTANT: backend auth also expects an `email` claim in the Clerk token.
# Clerk Dashboard setup:
#   1. Go to JWT Templates and create/use one template for this backend API.
#   2. Set template Audience to exactly this CLERK_AUDIENCE value.
#   3. In "Customize session token", include:
#        "email": "{{user.primary_email_address}}"
#   4. Make frontend request tokens from that template for protected API calls.
CLERK_AUDIENCE=https://api.yourapp.com
CLERK_JWKS_CACHE_TTL_SECONDS=3600
CLERK_JWT_LEEWAY_SECONDS=10
